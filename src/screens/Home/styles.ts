import styled from 'styled-components';

export const HomeContainer = styled.div`
  width: 100%;
  height: 100vh;
  background: linear-gradient(120deg, var(--bgGray1), var(--bgGray2));
  display: flex;
  justify-content: center;
  align-items: center;
`;
